# 필터·게이트 수정 시 기본 검증 절차

탐지 로직(`is_hard_excluded`, 패턴 목록, 프롬프트, 등급/발송 조건)을
건드릴 때는 **반드시 아래 순서를 지킨다.** 과거 이 절차를 건너뛰어
같은 유형의 사고가 반복됐다.

---

## 1. 수정 전 — 기준선 기록

```bash
python3 test_variants.py
```

현재 수치를 메모해 둔다. 수정 후 이 값과 비교한다.

## 2. 수정 후 — 필수 검증 (전부 통과해야 커밋)

```bash
python3 -m py_compile naver_news_monitor.py
python3 test_variants.py        # 종료코드 0이어야 함
```

`test_variants.py`가 한 번에 검사하는 것:

| 항목 | 통과 기준 | 의미 |
|---|---|---|
| **원본 미탐** | **0건** | 가장 중요. 진짜 리스크를 차단하면 안 됨 |
| 원본 오탐 차단 | 이전 이상 | 줄었으면 회귀 발생 |
| 변형 오탐 차단 | 16/16 | 종목명·표현만 바꿔도 걸려야 함(과적합 방지) |
| 변형 정탐 통과 | 16/16 | 변형된 진짜 리스크를 막으면 안 됨 |

## 3. 오탐 조치 시 — 변형 케이스를 함께 추가

오탐 하나를 막을 때 **그 기사만 막으면 과적합**이다.
`test_variants.py`의 `FP_VARIANTS`에 **같은 구조의 다른 표현**을 1건 이상
추가하고, 그것도 차단되는지 확인한다.

> 실제 사례: "영광의 IMA, 하지만 …쟁점"을 프롬프트 few-shot으로만 막았더니
> "화려한 STO 인가, 그러나 …쟁점"은 그대로 통과했다.
> → 개별 표현이 아니라 **문장 구조**(역접 + 논평어휘)로 판별하도록 수정.

## 4. 미탐 점검 — 오탐만 보면 절반만 보는 것

오탐은 메일에 보이므로 발견되지만, **미탐은 아무 흔적이 없다.**
호재·정상 패턴을 추가할 때는 그 패턴이 부정어와 결합한 경우를 반드시 시험한다.

> 실제 사례: `수주`·`계약체결`·`MOU`가 호재 패턴이라
> "계약체결 **무산**에 자금난", "수주 **취소**로 유동성 위기"까지 차단되고 있었다.
> 미탐이라 로그에 흔적이 없어 오래 발견되지 않았음.

## 5. 발송분 검토 후 — 회귀세트에 축적

메일을 검토해 오탐/정탐을 판정했으면 `regression_set.json`에 추가한다.
`entity` 필드를 반드시 넣는다(익스포저 기반 진단에 사용).

```json
{"title": "...", "entity": "...", "category": "판정 사유", "source": "YYYY-MM-DD HH시 발송 등급 점수"}
```

## 6. 커밋 — 코드와 데이터를 분리

```bash
git log --oneline -5
git diff origin/main --stat     # 의도한 파일만 변경됐는지 확인
```

- 코드/프롬프트 변경 → 한 커밋
- `regression_set.json`, `known_cases.json`, `exposure_data.csv` → 별도 커밋

---

## 측정할 때 지킬 것 (과거 오집계 방지)

1. **분모는 항상 입력 건수로 고정한다.**
   오류·스킵된 건을 분모에서 빼면 정확도가 실제보다 높게 나온다.
   → 과거 "정확도 36/36, 20/20"으로 보고했으나 실제로는 대부분 API 오류였고,
     정탐 20건은 **한 건도 응답을 못 받은 상태**였다.

2. **병렬 실행 결과는 예외까지 수집한다.**
   `future.result()`가 예외를 던지면 그 항목이 결과 목록에서 조용히 빠져
   분모가 줄어든다. 반드시 `try/except`로 감싸 오류도 결과로 기록한다.

3. **파이프라인 전체로 측정한다.**
   `is_hard_excluded`만 보면 실제와 다르다. `regrade_by_score`의 경쟁사 배제·
   당사 오추출 방어까지 거쳐야 최종 결과다.
   → 과거 "AI 의존 오탐 22건"으로 보고했으나 전체 파이프라인 기준으로는
     이미 상당수가 차단되고 있었다.

4. **집계 합이 입력과 맞는지 assert로 강제한다.**
   `정답 + 오판 + 오류 == 입력 건수`

---

## 진단 도구

| 파일 | 용도 | 발송 |
|---|---|---|
| `test_variants.py` | 변형+원본 회귀 (오프라인, 즉시) | 없음 |
| `diag_judgment.py` | 2차 검증 AI 판정 품질 (API 호출) | 없음 |
| `diag_keyword_volume.py` | 키워드 수집량 실측 (API 호출) | 없음 |

`diag_*`는 `.github/workflows/diag_keyword_volume.yml`을 통해
`workflow_dispatch`로 실행하며, 결과는 `diag_result.txt`로 커밋된다.

`manual_send.py`는 **전체 그룹메일로 실제 발송**된다.
`ARTICLES_PREPARED_ON`을 오늘 날짜로 갱신하고 `CONFIRM_SEND=YES`를
입력해야만 동작한다. 실행 전 반드시 내용을 확인받을 것.

---

# 발송 범위(전체발송 / 본인한정) 판정 규칙

임원 신뢰도에 직결되는 영역이므로 **이 표를 기준으로만 판단한다.**

## 판정식

```
_actionable = 긴급·주의 등급 기사만 (참고는 제외)
_max_score  = _actionable 중 최고 리스크점수 (없으면 0)

_force_full = 긴급 기사 존재
            OR (주의 + conf≥0.80 + 당사 익스포저 50억 이상)
            OR (위험고객 보유 -3%↓ 종목 ≥ 15개  AND  리스크잔고 ≥ 150억)

전체발송 = (_max_score >= 5.5) OR _force_full
본인한정 = 그 외
```

**임계값 (환경변수로 조정 가능)**

| 상수 | 기본값 | 의미 |
|---|---|---|
| `SELF_ONLY_MAX_SCORE` | **5.5** | 이 점수 이상이면 규모 무관 전체발송 |
| `STRONG_CAUTION_MIN_EXPOSURE` | **50**억 | conf 우회 경로의 최소 익스포저 규모 |
| `REF_FULLSEND_MIN_EXPOSURE` | **3,000**억 | 전체발송 메일에 실을 참고 등급의 최소 익스포저 |
| `MARKET_CRASH_STOCK_THRESHOLD` | **15**개 | 시장급락 강제발송 최소 종목 수 |
| `MARKET_CRASH_RBAL_THRESHOLD` | **150**억 | 시장급락 강제발송 최소 리스크잔고 (AND 조건) |

## 시장급락 강제발송 기준 재설계 (2026-07-27)

**종목 수와 리스크잔고 규모를 AND로 함께 본다.**

기존엔 -3%↓ 종목 수(10개)만 봐서, 하락장이면 뉴스 품질과 무관하게 매번
전사 발송됐다. 원인은 모집단과 분포:

| 항목 | 실측값 |
|---|---|
| 위험고객 보유 종목 | **305개** |
| 리스크잔고 합계 | 785억 |
| 종목당 **중앙값** | **0억** (상위 5종목이 54% 차지) |

모집단이 305개나 되어 평범한 조정장에도 10개는 쉽게 넘고, **잔고 0~1억짜리가
10개 떨어져도 전사 발송**됐다. 실제로 07/27 14시(10종목)·21시(14종목)가
연속 발동했다.

| 시나리오 | 종목 | 잔고 | 판정 |
|---|---|---|---|
| 07/27 14시 재현 | 10 | 35억 | 미발동 |
| 07/27 21시 재현 | 14 | 80억 | 미발동 |
| 평범한 조정장 | 18 | 45억 | 미발동 |
| **대형주 포함 하락** | 16 | 220억 | **전체발송** |
| **진짜 급락장** | 40 | 500억 | **전체발송** |
| 소수 대형주만 급락 | 8 | 300억 | 미발동 |

> '소수 대형주만 급락'이 미발동인 것은 사각지대가 아니다.
> 해당 종목에 뉴스가 잡히면 점수·conf 경로로 별도 발송된다.

## 전체발송 시 '참고' 등급 축소 (2026-07-27)

**전체 발송 메일에는 익스포저 3,000억 이상인 참고만 싣는다.**
본인 한정 메일에는 참고를 전부 유지하므로 담당자 모니터링에는 공백이 없다.

**근거 (최근 9회 발송 전수 실측)**

| 등급 | 정탐 | 오탐 | 오탐률 |
|---|---|---|---|
| 긴급 | 0 | 0 | — |
| 주의 | 2 | 2 | 40% |
| **참고** | 5 | **18** | **78%** |

전체 오탐 20건 중 **18건(90%)이 참고 등급**이었다.
참고는 메일에도 '직접 손실 없는 동향 — 참고 파악용'으로 표기되어 있어
임원 판단에 필수가 아닌데, 오탐의 대부분을 여기서 만들어내고 있었다.

**임계 선정 실측** (오늘 신설 게이트 적용 후 잔존 참고 기준)

| 기준 | 정탐 | 오탐 |
|---|---|---|
| 제한 없음 | 3 | 3 |
| 500억 이상 | 3 | 1 |
| **3,000억 이상** | **3** | **0** |

3,000억에서 정탐 3건(삼성전자·SK하이닉스·마이크론 대형 급락)이 전부
보존되면서 오탐 노출이 0이 된다.

**전체발송 회차 재현 결과**

| 회차 | 뉴스 건수 | 오탐 | 정탐 |
|---|---|---|---|
| 07/27 14시 | 4 → 1 | 3 → **0** | 1 → 1 |
| 07/27 21시 | 5 → 0 | 5 → **0** | 0 → 0 |
| 07/25 07시 | 4 → 3 | 2 → 1 | 2 → 2 |

> 07/27 21시처럼 참고만 있던 회차는 뉴스 섹션이 비게 되는데,
> 그 회차의 전체발송 사유는 '시장급락 14종목'이었으므로
> 여신잔고 리스크 현황이 메일 본문 역할을 한다.

**2026-07-25 상향 이력 (임원 대상이므로 고위험만 발송)**
최근 9회 발송 실적을 재현해보니 전체발송이 **9/9(100%)**로 과다했다.
점수 임계만 올려도 효과가 없는데, `conf≥0.80` 우회 경로가 점수 조건을
그냥 통과시키기 때문이다. 실효 있는 지점은 **우회 경로의 익스포저 규모 요건**이었다.

| 조합 | 전체발송 | 정탐 영향 |
|---|---|---|
| 기존(5.0 / 익스포저>0) | 9/9 (100%) | — |
| **채택: 5.5 / 익스 50억** | **6/9 (67%)** | ✅ 없음 |
| (검토) 6.0 / 익스 100억 | 5/9 (56%) | ❌ 엔비티(정탐) 누락 |

차단된 3건: 예선테크(4.2점·2억·여신없음), 서천특화시장(4.2점·0억),
시디즈(5.0점·15억). 유지된 정탐 4건: 제이알글로벌리츠(7.3·1,274억),
엔비티(5.5·19억), 코오롱티슈진(5.0·388억), 대호에이엘(5.2·100억).

> 소규모 종목의 진짜 리스크(엔비티 19억)가 계속 커버되는 이유:
> **점수 5.5 이상은 익스포저 규모와 무관하게 전체발송**된다.
> 규모 요건은 '점수 미달인데 conf로 우회하는' 경로에만 적용된다.

## 진리표 (검증 완료)

| 상황 | 최고점 | 판정 |
|---|---|---|
| 긴급 기사 존재 | 점수 무관 | **전체발송** |
| 주의, 점수 ≥ 5.5 | 5.5↑ | **전체발송** |
| 주의, 점수 < 5.5, 익스포저 **50억↑**, conf ≥ 0.80 | 4.2 | **전체발송** |
| 주의, 점수 < 5.5, 익스포저 50억 미만(예: 2억) | 4.2 | 본인한정 |
| 주의, 점수 < 5.5, **익스포저 X** | 4.2 | 본인한정 |
| 주의, 점수 < 5.5, conf < 0.80 | 5.4 | 본인한정 |
| **참고만** (점수 아무리 높아도) | 9.0 → 0 | 본인한정 |
| 참고만 + 시장급락 10종목↑ | — | **전체발송** |
| 참고만 + 시장급락 9종목 | — | 본인한정 |

### `conf`(confidence)의 의미

AI가 매기는 **"이 기사가 진짜 리스크 기사라는 확신도"**(0.0~1.0)이며,
**손실 규모가 아니라 판단의 자신감**이다. `filter_prompt.txt`의 캘리브레이션 기준:

| 구간 | 기준 |
|---|---|
| 0.70 미만 | 익스포저 없는 종목·사건 → `relevant:false` |
| 0.60 이상 | 익스포저 확인 종목 → `true` 가능 |
| 0.60 이하 **고정** | 칼럼·시리즈·거시전망·경영권분쟁·기지사건 회고 |
| **0.80 이상** | 전체발송 트리거(단, 익스포저 실재 시에만) |

`conf ≥ 0.80` 조건의 목적은 *"점수는 5점 미만이어도 AI가 확실하다고 본
주의 건은 놓치지 말자"*는 미탐 방지다. 다만 conf가 높다고 손실이 큰 것은
아니므로 **익스포저 실재 요건을 반드시 함께 본다.**

**핵심 원칙**
- 참고 등급은 `_actionable`에서 제외되므로 점수가 높아도 전체발송을 유발하지 않는다
  (경쟁사 자체 리스크가 참고로 강등된 뒤 익스포저 때문에 점수만 높은 경우 방지).
- 익스포저가 없으면 conf가 아무리 높아도 전체발송하지 않는다
  (conf는 'AI의 확신도'일 뿐 손실 규모가 아님).

## 뉴스 0건일 때 (별도 경로)

```
전체발송 = 경보 종목 ≥ 5개  OR  위험고객 리스크잔고 합계 ≥ 50억
```

| 상황 | 판정 |
|---|---|
| 경보 없음 | 본인한정 (`send_email_no_result`, 빈 메일) |
| 1종목·3억 | 본인한정 (가격경보는 포함) |
| 4종목·49억 | 본인한정 (가격경보는 포함) |
| 5종목·20억 (종목수 충족) | **전체발송** |
| 2종목·95억 (잔고 충족) | **전체발송** |
| 12종목·300억 | **전체발송** |

**설계 근거 (2026-07-25 조정)**
- 기존엔 경보 종목 1개만 있어도 전체발송이었다. 위험고객 보유 종목이
  **303개**라 그중 하나만 -3% 하락해도 발동해, 평상시에도 거의 매일
  전사 메일이 나갈 수 있는 설정이었다.
- 잔고 조건을 병행하는 이유: 실측상 단일 종목 최대 95억, 상위 3종목이
  전체 리스크잔고의 45%를 차지한다. '적은 종목 수 + 큰 잔고' 상황을
  종목 수만으로는 놓치기 때문.
- **기준 미달이어도 본인에게는 가격경보가 그대로 발송된다.**
  발송 '범위'만 가르고 '내용'은 동일 — 정보가 사라지지 않게 하기 위함.

뉴스가 있을 때의 시장급락 임계(10종목)와 기준이 다른 것은 의도된 설계다.
뉴스 0건일 때는 메일 내용이 여신잔고 현황 그 자체라 더 낮은 기준을 쓴다.

## 수신자 검증 (실측 완료)

| 모드 | 실제 수신자 |
|---|---|
| `self_only=True` | 발신자 본인 1명만 — 그룹·CC 유출 없음 |
| `self_only=False` | 본인 + `EMAIL_RECEIVER` + `EMAIL_CC` (중복 자동 제거) |
| `send_email_no_result` | `NO_RESULT_RECEIVER`만 |

## 발송 로직 수정 시 필수 확인

1. 위 진리표 14개 시나리오를 전부 재현해 판정이 바뀌지 않았는지 확인
2. `self_only=True`에서 그룹 주소가 하나도 나가지 않는지 SMTP 모의로 실측
3. Actions 로그의 `[발송판정]` 줄만 보고 판정 근거를 재구성할 수 있는지 확인
   (강제발송 조건은 실제 발동한 것만 표기되어야 함)


---

# 공용 헬퍼 사용 원칙 (필수)

점검할 때마다 같은 유형의 결함이 반복된 원인은 **한 곳을 고쳐도 동일 패턴의
다른 지점이 남아 있어서**였다. 아래 헬퍼를 쓰면 같은 유형이 재발하지 않는다.

## 1. 키워드 매칭은 반드시 `_kw_hit()`

```python
# ❌ 쓰지 말 것 — 띄어쓰기가 다르면 못 잡는다
if any(k in title for k in SOME_KEYWORDS):

# ✅ 이렇게
if _kw_hit(title, SOME_KEYWORDS):
if _kw_hits(title, SOME_KEYWORDS):   # 매칭된 키워드 목록이 필요할 때
```

**이유:** 한국어 기사 제목은 같은 표현도 띄어쓰기가 제각각이다.
원문 비교만 하면 같은 사건이 다르게 판정된다.

| 실측 사례 | 원문 비교 | 결과 |
|---|---|---|
| "전산 장애" vs "전산장애" | 다름 | 점수 **5.0 vs 8.2** |
| "키움 증권 MTS 장애" | 경쟁사 미인식 | 배제 실패 |
| "완전 자회사" vs "완전자회사" | 다름 | 게이트 통과 |
| "응원 매수" vs "응원매수" | 다름 | 면제 미적용 |

전수 스캔 시점에 매칭 지점 27곳 중 **24곳이 미적용**이었다.
새 키워드 조건을 추가할 때는 예외 없이 `_kw_hit`을 쓴다.

## 2. 숫자 변환은 반드시 `_num()`

```python
# ❌ 쓰지 말 것 — 빈 값 하나에 전체가 죽는다
bal = float(str(r.get("잔고(억)", "0")).replace(",", ""))

# ✅ 이렇게
bal = _num(r.get("잔고(억)"))
cnt = int(_num(r.get("고객수")))
```

**이유:** `exposure_data`는 대직자가 엑셀에서 변환해 올리는 **외부 데이터**라
빈 값·하이픈·결측이 섞일 수 있다. 방어 없이 쓰면 잔고에 `''` 하나만 들어와도
`calc_risk_score`와 `build_exposure_html`이 `ValueError`로 중단되어
**그 회차 메일이 통째로 유실된다.**

`_num`이 방어하는 입력: `'1,234'` · `''` · `None` · `'-'` · `'nan'` · 공백 · 비수치

## 3. AI 응답값은 범위를 신뢰하지 않는다

`confidence` 등 AI가 돌려주는 값은 외부 입력이다. 음수·1 초과·문자열이
들어올 수 있으므로 사용 전 클램프한다.

```python
conf = min(max(float(conf), 0.0), 1.0)   # 변환 실패 시 보수적 기본값
```

> 실측: `conf=-0.5` → 리스크점수 **-2.8** (0~10 범위 이탈)

## 4. HTML 출력은 `_esc()` 경유

`_esc()`는 `None`·숫자 등 비문자열도 안전하게 처리한다.
`html.escape()`를 직접 쓰면 AI 응답의 `null`에서 이메일 생성이 죽는다.

---

## 새 매칭·변환 코드를 추가했을 때 자가 점검

```bash
# 공백무시 미적용 매칭이 남아 있는지
grep -nE 'any\(\s*\w+ in (title|_title|text|_halt_text)' naver_news_monitor.py

# 방어 없는 숫자 변환이 남아 있는지 (주석 1건은 헬퍼 설명이므로 정상)
grep -n 'float(str(' naver_news_monitor.py
```

첫 번째는 결과가 0건, 두 번째는 `_num` 정의부의 설명 주석 1건만 나와야 한다.
그 외가 나오면 `_kw_hit` / `_num`으로 교체한다.

> `or '0'` 방어로는 부족하다. `float(str(v or '0')...)` 는 `''`·`None`은
> 막지만 `'-'`·`'abc'` 같은 비수치 문자열에서는 그대로 `ValueError`가 난다.
> (실측 확인 — 이 형태 8곳도 `_num`으로 전환함)


---

# 운영 의존성 (변경 시 함께 봐야 하는 것)

## 스케줄 ↔ 수집 윈도우는 연동돼 있다

현재: **매일(주말 포함) 07 / 14 / 21시 KST**, 수집 윈도우 **14시간**

| 구간 | 간격 | 14h 창 커버 |
|---|---|---|
| 07 → 14시 | 7h | ✅ |
| 14 → 21시 | 7h | ✅ |
| 21 → 익일 07시 | 10h | ✅ |

**주말 실행을 끄면 안 된다.** 금 21시 → 월 07시 = **58시간** 공백이 되어
14시간 창으로는 대량 누락이 발생한다. 주말 실행을 중단하려면 수집 윈도우를
60시간 이상으로 함께 늘려야 한다.

> 주말 실행의 실효: **뉴스 탐지만 유효**하다. 가격경보·가격연동 재발송(-8%)은
> 당일 시세 데이터가 없어 자동 스킵된다(코드에 '오늘 데이터 아니면 미표시').
> 즉 주말 메일은 뉴스 기반 판정만 담긴다.

## 스케줄을 다시 바꿀 때 확인할 것

1. 최대 실행 간격을 계산하고, 수집 윈도우가 그보다 넉넉한지 확인
2. `crawl_naver_news()`의 `timedelta(hours=14)` 갱신
3. 트리거는 cron-job.org에 있으므로 레포만 고쳐도 반영되지 않음

## API 한도 여유 (실측)

| 항목 | 현황 | 한도 | 여유 |
|---|---|---|---|
| Gemini 1차 | 수집 236~315건 → 5~7요청/회 | 15 RPM (무료) | ✅ 충분 |
| Claude | 회당 5~8회 (등급재검증 1 + 2차검증 N + action) | — | 비용 영향 미미 |
| 키워드 | 24개 | — | 단일 키워드 최대 77건, 페이지네이션 상한 300건 대비 여유 |

키워드를 크게 늘리거나 배치 크기를 줄이면 Gemini RPM을 다시 계산할 것.

## 파일 크기 (실측)

| 파일 | 크기 | 비고 |
|---|---|---|
| `exposure_data.csv` | 698KB | 매일 교체(누적 아님) |
| `dart_corp_codes.json` | 267KB | 정적 |
| `ticker_map.json` | 73KB | 주간 갱신 |
| `seen_news.json` | 19KB | 72h 보존으로 자동 정리(최대 ~9슬롯) |
| `.git` | 14MB | 매일 CSV 교체 커밋으로 점진 증가 — 연 1회 정도 확인 |

---

# 오탐·오류 패턴 정리 (2026-07 운영 기록)

## A. 반복된 구조적 사고 — "규칙은 있는데 무력화됨" (5회)

가장 많이 반복된 유형. `CRITICAL_KW`(상장폐지·파산·부도·거래정지·회생 등)가
제목에 있으면 bypass가 먼저 `return`하므로, **그 뒤에 있는 규칙은 실행되지 않는다.**

| 회차 | 뚫린 기사 | 등재돼 있던 규칙 |
|---|---|---|
| 1 | SK시그넷 상폐·프리미엄 공개매수 (긴급 6.5) | filter_prompt 3곳 |
| 2 | 한울앤제주 주식분할 거래정지 (긴급 6.8) | — |
| 3 | 효성화학 거래정지 해제·70% 급등 (참고 6.0) | '거래재개' 패턴 |
| 4 | 파산 홈플러스 vs 흑자 남양유업 (참고 4.2) | — |
| 5 | [더벨][상장폐지 카운트다운] (참고 5.8) | 정규식 등재돼 있었음 |

**대책:** `is_hard_excluded`를 4단 구조로 명시(코드 상단 주석 참조).
새 게이트는 **반드시 3단(bypass)보다 앞**에 배치한다.

## B. 문자열 매칭 결함

| 유형 | 사례 | 대책 |
|---|---|---|
| 띄어쓰기 변형 | '전산 장애' vs '전산장애' → 점수 5.0 vs 8.2 | `_kw_hit()` 공백 무시 |
| 짧은 패턴 오매칭 | 'OO사 자금난'→'OO사자금난'에 '사자' 매칭 (미탐) | `_NS_EXEMPT` 예외 |
| 부분일치 오인 | '[제약공시 책갈피]'가 화이트리스트 '공시'에 매칭 | 완전일치로 변경 |
| 호스트 전체 등재 | `tenasia.hankyung.com`만 등재 → `tenasia.co.kr` 통과 | 핵심 도메인(`tenasia.`)으로 |
| 제목 잘림 | '[Z-스코어 기업가치 ...' 닫는 괄호 소실 | 말줄임 패턴 추가 인식 |

## C. 호재를 리스크로 오인

공개매수 프리미엄 / 완전자회사 편입 / 거래정지 해제 / 회사채 증액 확정 /
어닝서프라이즈 / 수익률 상위 / 인가 획득 / 응원매수 / 합병 완료

> **반대 방향 주의:** 호재 패턴을 추가하면 '호재 무산' 기사가 미탐이 된다.
> `수주 취소`·`계약체결 무산`·`MOU 파기`·`깜짝 실적에도 급락` 등.
> → `_POSITIVE_PATS` + `_NEGATION_KW` 조합으로 면제 처리.

## D. 당사 무관 주체

경쟁 증권사·금융지주 자체 이슈 / 시장 인프라 기관(거래소·예탁원) 행정 /
해외 비상장 파산 / K-OTC 장외시장 / 거시 전망 칼럼 / 연예·인물 가십

## E. 발송 로직 사고

| 사고 | 원인 |
|---|---|
| 5점 미만인데 전체발송 | conf≥0.80 우회 경로가 익스포저 규모를 안 봄 |
| 하락장마다 전체발송 | 시장급락 기준이 종목 수만 봄(모집단 305개, 중앙값 0억) |
| **급락장인데 본인한정** | **HTML 생성을 판정 뒤로 옮겨 집계값이 0으로 읽힘** |
| 동일 메일 2회 발송 | 수동 발송 재실행 시 확인 절차 없었음 |

> 마지막 항목이 가장 위험했다. 함수 속성(side effect)으로 값을 주고받는
> 구조는 **호출 순서가 바뀌면 조용히 깨진다.** 순서 의존 코드를 옮길 때는
> side effect 생산자·소비자의 상대 위치를 반드시 확인할 것.

## F. 데이터·런타임

| 유형 | 사례 |
|---|---|
| 단위 오류 | 담보비율 소수(1.46) ↔ 퍼센트(146) 혼입 |
| 이상치 | 담보비율 0.0 → '최고위험 고객 0%'로 오독 |
| 외부 입력 미검증 | AI `confidence` 음수 → 점수 -2.8 |
| 결측 방어 없음 | 잔고 `''` 하나로 메일 생성 전체 중단 |
| 이름 변형 | 같은 법인이 채권 2줄로 중복 표시 |

---

## 새 오탐을 만났을 때 체크리스트

1. **어느 단계에서 놓쳤나?** 규칙이 이미 있는데 안 걸렸다면 → A유형, 배치 확인
2. **띄어쓰기·잘림 변형인가?** → B유형, `_kw_hit` 사용 여부 확인
3. **호재인가?** → C유형, 단 부정어 결합 미탐을 반드시 함께 시험
4. **당사와 무관한 주체인가?** → D유형
5. 조치 후 **변형 케이스를 `test_variants.py`에 함께 추가** (과적합 방지)
6. `regression_set.json`에 `entity` 포함해 축적

---

# 검수 페르소나 (정기 점검용)

주기적 점검 시 아래 두 관점을 각각 적용한다. 관점이 다르면 보이는 결함도 다르다.

## 페르소나 A — 임원 (오탐·오류 무관용)

> "잘못된 알림 하나가 봇 전체의 신뢰를 무너뜨린다.
>  알림이 안 온 것보다, 틀린 알림이 온 게 더 나쁘다."

**점검 질문**
1. 이번 주 발송된 메일에 틀린 내용이 있었나? 있었다면 등급은?
2. 알려야 할 상황(급락·부도)에 메일이 안 갔거나 본인한정에 그친 적은?
3. 같은 유형 오탐이 재발했나? (재발 = 대책이 형식적이었다는 뜻)
4. 메일만 보고 판단할 수 있나? 숫자·표기가 오독을 유발하지 않나?
5. 왜 그렇게 판정했는지 로그로 재구성 가능한가?

**임원 기준 심각도**

| 등급 | 정의 | 실제 사례 |
|---|---|---|
| 치명 | 알려야 할 때 안 감 / 긴급 등급 오탐 | 급락장 전사 미발송, 긴급 6.8 주식분할 오탐 |
| 높음 | 중복 발송 / 주의 등급 오탐 | 동일 메일 2회, 완전자회사 편입 주의 |
| 중간 | 참고 오탐 / 저가치 전사 발송 | 참고 오탐 다수, 하락장 자동발송 |

## 페르소나 B — 시니어 개발자 (구조·데이터 튜닝)

> "동작하는 코드와 유지보수 가능한 코드는 다르다.
>  오늘 고친 방식이 내일 새 버그를 만들지 않는가?"

**점검 질문**
1. 순서에 의존하는 코드가 있나? side effect 생산자·소비자가 떨어져 있나?
2. 자동 검증이 닿지 않는 영역은? 사고는 늘 거기서 난다.
3. 함수가 너무 길거나 분기가 많아 수정 영향을 예측할 수 없나?
4. 실패가 조용히 넘어가는 지점은? 원인 추적이 가능한가?
5. 임계값이 코드에 흩어져 있나, 상수·환경변수로 모여 있나?

---

# 페르소나 기반 점검 결과 (2026-07-28)

## 발견 — 구조적 취약점

| # | 항목 | 실측 | 조치 |
|---|---|---|---|
| 1 | **자동 검증 사각지대** | 변형테스트가 `is_hard_excluded` 단일 함수만 검증. 발송판정·등급조정·점수계산 미검증 | ✅ `test_send_decision.py` 신설(13케이스) |
| 2 | **순서 의존 side effect** | `build_price_alert_section`이 함수 속성에 기록 → 멀리 떨어진 곳에서 `getattr` | ✅ 판정 직전 명시 호출 + 테스트로 고정 |
| 3 | 조용한 실패 | `except: pass` 15곳 | ✅ 핵심 경로(가격조회·익스포저 요약) 로그 추가 |
| 4 | 거대 함수 | `main` 1,071행/분기 241, `is_hard_excluded` 460행/분기 65 | 🔸 4단 구조 주석으로 완화(분할은 미실시) |
| 5 | 전역 가변 상태 | `TITLE_ONLY_PATTERNS += ` 로 후속 변경 | 🔸 사전계산(`_TITLE_PATTERNS_NS`)이 += 이후에 위치함을 확인 |

## 검증 커버리지 현황

| 대상 | 도구 | 케이스 |
|---|---|---|
| `is_hard_excluded` | `test_variants.py` | 변형 79 + 원본 88 |
| **발송판정·참고축소·시장급락** | **`test_send_decision.py`** | **13** |
| 2차 검증 AI 판정 | `diag_judgment.py` | 회귀세트 88 (API 호출) |
| **`regrade_by_score`** | **`test_regrade.py`** | **9** |
| `calc_risk_score` | — | ⚠️ 미검증(단, 범위 클램프는 적용됨) |
| HTML 생성 | — | ⚠️ 수동 검수만 |

`test_regrade.py`가 고정하는 것: 경쟁사 자체 리스크 강등/배제(공백 변형 포함),
당사 이슈 유지, 당사 오추출 시 entity 무효화, 피해종목 별도인 경우 유지,
익스포저 없는 종목의 긴급 등급 상한.

## 수정 전 필수 실행 (2개로 확대)

```bash
python3 test_variants.py        # 탐지 규칙 (변형 79 + 원본 88)
python3 test_send_decision.py   # 발송 판정 (13)
python3 test_regrade.py         # 등급 조정 (9)
```

**셋 다 종료코드 0이어야 커밋한다.**

## 여신잔고 리스크 현황 표 열 순서 (2026-07-28 변경)

```
종목명 → 전체 여신 → ⚠ 위험고객 → 최고 리스크
```

모수(전체 여신)를 먼저 보여주고 그 부분집합(위험고객)을 이어서 배치한다.
기존에는 위험고객이 앞에 있어 규모 대비 감을 잡기 어려웠다.
